<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Logout - Service Saathi</title>
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 flex items-center justify-center h-screen">

    <div class="bg-white shadow-xl rounded-lg p-8 w-96 text-center">
        <h2 class="text-2xl font-semibold text-gray-800">Are you sure?</h2>
        <p class="text-gray-600 mt-2">You will be logged out of your account.</p>

        <div class="mt-6 flex flex-col gap-3">
            <button id="logout-btn" class="w-full bg-blue-500 text-white px-6 py-2 rounded-md hover:bg-blue-600 transition">
                Logout
            </button>
            <a href="/dashboard" class="bg-gray-300 text-gray-800 px-6 py-2 rounded-md hover:bg-gray-400 transition">
                Cancel
            </a>
           
        </div>
    </div>
            <script>
                document.getElementById("logout-btn").addEventListener("click", function() {
            fetch("/logout", { method: "POST" })
            .then(response => response.json())
            .then(data => {
                if (data.success) {
                    window.location.href = "/"; // Redirect to login page after logout
                } else {
                    alert("Logout failed. Please try again.");
                }
            })
            .catch(error => console.error("Logout error:", error));
        });
            </script>
       
    
</body>
</html>
